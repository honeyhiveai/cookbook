# Required for baseline (Step 4)
OPENAI_API_KEY=
# Optional — defaults to gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# HoneyHive — https://app.honeyhive.ai/settings/project/keys
HH_API_KEY=
HH_PROJECT=
# Dedicated / self-host only — omit to use the multi-tenant default
HH_API_URL=
